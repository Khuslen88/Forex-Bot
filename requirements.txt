# PyTorch — CPU-only wheel (Streamlit Cloud has no GPU; CUDA build seg-faults)
# torch 2.4+ supports numpy 2.x, which the production models were pickled with.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu

# Data — numpy 1.x for ABI/SIGSEGV safety with torch 2.3.1.
# dashboard.py installs a numpy._core compat shim so pickles saved with
# numpy 2.x (production models) can still unpickle here.
numpy>=1.24.0,<2.0
pandas>=2.0.0,<3.0
yfinance>=0.2.0

# Economic Data
fredapi>=0.5.0

# Technical Indicators
ta>=0.10.0

# Signal Processing (DSP-based denoising, ported from Modern Topics in DS project)
PyWavelets>=1.4.0       # wavelet decomposition (Zhao & Khushi 2021) — production
# EMD-signal>=1.4.0     # optional, omitted for cloud deploy (production uses wavelet)
scipy>=1.10.0           # paired t-test + Wilcoxon for statistical_tests.py

# Sentiment Analysis
textblob>=0.17.0

# Machine Learning / RL
stable-baselines3>=2.0.0
gymnasium>=0.29.0
# torch pinned at top (CPU-only wheel)

# Visualization
plotly>=5.15.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Dashboard
streamlit>=1.25.0
streamlit-autorefresh>=0.1.0

# Broker
requests>=2.31.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.65.0

# Development / local only
pytest>=7.0.0
